# Gym Application Configuration

# Storage Configuration (obsolete)
# storage.data.file.path=classpath:data/initial-data.json

# Hibernate DB config is in HibernateConfig.java
# To switch to MySQL: replace EmbeddedDatabase with DriverManagerDataSource + MySQLDialect
